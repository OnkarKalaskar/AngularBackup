export interface PersonalDetails{

    userId: string | null,
    fname: string | null,
    lname: string | null,
    dateOfBirth: string | null,
    email: string | null,
    password: string | null,
    gender: string | null,
    address: string | null,
    pincode: number | null,
    mobileNumber: number | null

}